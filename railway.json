{
  "scripts": {
    "check": "python3 check_tls.py"
  },
  "crons": [
    {
      "schedule": "*/20 * * * *",
      "command": "check"
    }
  ],
  "build": {
    "commands": [
      "pip install -r requirements.txt",
      "playwright install"
      "chmod +x start.sh"
    ]
  }
}
